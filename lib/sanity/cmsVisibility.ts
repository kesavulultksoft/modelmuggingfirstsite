/**
 * Toggle visibility for CMS-driven UI the client asked to pause.
 * Set to true when Call-to-action blocks should appear in Studio and on the site again.
 */
export const SHOW_PAGE_CTA_SECTION = false

/**
 * Men’s Basic only: mid-page “Plan your next step” media band + hardcoded footer signup block.
 * Keep in sync with `mmcms/constants/cmsVisibility.ts` for Studio.
 */
export const SHOW_MENS_BASIC_EMBEDDED_CTAS = false
