import { formatTitleCase } from '@/lib/formatTitleCase'

/** Title-case labels for location page buttons and box headings (each word capitalized). */
export const formatLocationLabel = formatTitleCase
